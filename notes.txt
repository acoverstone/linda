HOW TO SET UP:
	sudo apt-get update
	manually download pyaudio 0.2.9 
	sudo apt-get install swig3.0 sox
	sudo apt-get install libatlas-base-dev
	sudo pip install SpeechRecognition
	sudo apt-get install espeak
	sudo pip install pyttsx
	sudo pip install pyowm

HOW TO RUN CODE:
	python linda.py


TODO:
	Create how-to for setup on pi (maybe makefile/script)
	Connect recognizer and speech recognition, set up main structure loop
	Train wit AI
	Create commands
		